query GovernedResourceAutomaticRulesQueryJob($id: ID!) {
    governedResourceAutomaticRulesQueryJob(jobId: $id) {
        jobId
        jobStatus
        jobProgress
        results {
            id
        }
    }
}
