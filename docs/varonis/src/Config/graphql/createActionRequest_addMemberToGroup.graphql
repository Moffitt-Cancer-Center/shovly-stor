mutation CreateActionRequest($input: CreateActionRequestInput!) {
    createActionRequest(
        input: $input
    ) {
        requestResults {
            actionRequestId
            failureMessage
            status
        }
    }
}
