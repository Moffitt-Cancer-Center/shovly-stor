query GovernedGroupsQueryJob ($id: ID!){
    governedGroupsQueryJob(jobId: $id) {
        results {
            group {
                samAccountName
                directoryServices {
                    name
                }
            }
        }
    }
}
