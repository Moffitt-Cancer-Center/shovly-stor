mutation AddGovernedGroupOwnersAsync
(
    $groupName: String!
    $groupDomain: String! 
    $ownerName: String!
    $ownerDomain: String! 

)
{
    addGovernedGroupOwnersAsync(
        addGovernedGroupOwnersInput: {
            group: {
                samAccountName: $groupName
                directoryServices: { name: $groupDomain }
            }
            owners: {
                samAccountName: $ownerName
                directoryServices: { name: $ownerDomain }
            }
        }
    ) {
        jobId
        jobStatus
        results {
            succeeded
            extensions {
                errorMessage
                errorDetails
                errorCode
            }
        }
    }
}
