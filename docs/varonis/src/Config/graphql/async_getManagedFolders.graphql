query GovernedResourcesAsync {
    governedResourcesAsync(where: { isGoverned: { eq: true } }) {
        jobId
        results {
            id
            displayPath
            isUndetected
            isAllowDirectAcl
            isRootResource
            isGoverned
            owners {
                id
                samAccountName
            }
             authorizers {
                level
                account {
                    id
                    samAccountName
                }
            }
        }
        jobStatus
    }
}
